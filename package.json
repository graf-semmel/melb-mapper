{
  "name": "melbmaps",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build --base \"/melb-mapper\"",
    "preview": "vite --open preview",
    "format": "npx @biomejs/biome format --write <files>",
    "lint": "npx @biomejs/biome lint --write <files>",
    "check": "npx @biomejs/biome check --write <files>"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.17",
    "leaflet": "^1.9.4"
  }
}
